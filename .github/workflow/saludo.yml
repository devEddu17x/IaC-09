name: Saludo
on:
  push:
    branches:
      - main
      - develop
jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: Saludo desde main
        run: echo "¡Hola, desde main soy Eddu"
        if: github.ref == 'refs/heads/main'
        
      - name: Saludo desde develop
        run: echo "¡Hola, desde develop soy Eddu"
        if: github.ref == 'refs/heads/develop'